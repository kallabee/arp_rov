[Unit]
Description=ROV ROS 2 jazzy container
Requires=docker.service
After=docker.service

[Service]
Restart=always
RestartSec=3
ExecStart=/usr/bin/docker start -a jazzy
ExecStop=/usr/bin/docker stop -t 15 jazzy

[Install]
WantedBy=multi-user.target
